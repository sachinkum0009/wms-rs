# WMS Core

This crates handles the core functionalities of WMS.

CLI can be used to publish and subscribe to topics through MQTT.

## Usage

```bash
wms_cli pub -t hello -m world

wms_cli sub -t hello
```